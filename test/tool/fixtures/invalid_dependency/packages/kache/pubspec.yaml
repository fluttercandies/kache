name: kache
description: Fixture with a forbidden Flutter dependency in the Dart-only core.
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
