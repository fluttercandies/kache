name: kache_hive_ce
description: Fixture using a hosted source for a workspace dependency.
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  hive_ce: ^2.19.3
  kache:
    hosted: https://example.invalid
    version: ^0.1.0
