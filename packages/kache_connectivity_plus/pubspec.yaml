name: kache_connectivity_plus
description: connectivity_plus network recovery integration for Kache.
version: 1.0.0
homepage: https://github.com/fluttercandies/kache
repository: https://github.com/fluttercandies/kache/tree/main/packages/kache_connectivity_plus
issue_tracker: https://github.com/fluttercandies/kache/issues
topics:
  - cache
  - connectivity
  - network
  - offline-first

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  connectivity_plus: ^6.1.5
  flutter:
    sdk: flutter
  kache: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
