name: kache_example_support
description: Shared non-published runtime and UI for the Kache example apps.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  hive_ce_flutter: 2.3.4
  http: 1.6.0
  kache: ^1.0.0
  kache_connectivity_plus: ^1.0.0
  kache_flutter: ^1.0.0
  kache_hive_ce: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
