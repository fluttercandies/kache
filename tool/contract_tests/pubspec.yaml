name: kache_contract_tests
version: 1.2.0
description: Shared non-published contract tests for Kache packages and adapters.
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  kache: ^1.2.0
  kache_bloc: ^1.2.0
  kache_connectivity_plus: ^1.2.0
  kache_flutter: ^1.2.0
  kache_flutter_hooks: ^1.2.0
  kache_hive_ce: ^1.2.0
  kache_hooks_riverpod: ^1.2.0
  kache_provider: ^1.2.0
  kache_riverpod: ^1.2.0
  riverpod: ^3.3.2
  test: ">=1.26.2 <1.32.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
