name: kache_provider_example
version: 1.1.0
description: Example application for the Provider integration provided by Kache.
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  kache_example_support: ^1.1.0
  kache_provider: ^1.1.0

dev_dependencies:
  flutter_cockpit: ^1.1.3
  flutter_test:
    sdk: flutter
  kache: ^1.1.0
  kache_hive_ce: ^1.1.0

flutter:
  uses-material-design: true
